apiVersion: v1
kind: Pod
metadata:
  name: worker-default
spec:
  containers:
    - name: base
      image: {{ cookiecutter.image_repo }}:{{ cookiecutter.project_slug }}-base
      resources:
        requests: { cpu: "250m", memory: "512Mi" }
        limits:   { cpu: "1",    memory: "2Gi" }
