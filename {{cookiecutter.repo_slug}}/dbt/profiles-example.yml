{{cookiecutter.repo_slug}}:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      user: airflow
      password: airflow
      dbname: airflow
      port: 5432
      schema: public
